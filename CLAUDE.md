# הנחיות עבודה בפרויקט הזה

## זרימת העבודה שהמשתמש הגדיר

### 1. תיקיית «תשובות»

**כל תשובה שאני נותן נשמרת כקובץ טקסט בתיקיית `תשובות/`** - בלי יוצא מן הכלל,
גם כשמדובר בתשובה קצרה או בשאלת הבהרה.

שם הקובץ: `NNN - YYYY-MM-DD - <תיאור קצר>.txt`
כאשר NNN הוא מספר רץ תלת-ספרתי לפי סדר כרונולוגי.

לפני כתיבת קובץ חדש: לבדוק את המספר האחרון בתיקייה ולהמשיך ממנו.
לא לדרוס קבצים קיימים.

התוכן הוא התשובה עצמה בטקסט רגיל, קריא בלי עיצוב מיוחד.

### 2. תיקיית «שינויים במערכת»

- `שינויים במערכת/דברים לשנות.txt` - המשתמש כותב כאן מה לשנות.
- `שינויים במערכת/היה/` - ארכיון הבקשות שכבר בוצעו.

**כשהמשתמש אומר "רוץ"** (או כל וריאציה: "תרוץ", "בצע", "לך"):

1. לקרוא את `דברים לשנות.txt`.
2. להתעלם משורות שמתחילות ב-`#`.
3. אם משהו לא ברור או שיש לו כמה פירושים - **לשאול לפני שנוגעים בקוד**.
4. לבצע את השינויים.
5. להריץ `make test` ולוודא שהכל עובר. אם משנים נתונים - גם `make`.
6. להעביר את הקובץ ל-`היה/` בשם `YYYY-MM-DD - NNN - <תיאור>.txt`,
   עם סיכום קצר בראשו: מה התבקש, מה בוצע, ומה לא בוצע ולמה.
7. ליצור `דברים לשנות.txt` **ריק לחלוטין** - בלי כותרת, בלי הסבר,
   בלי תבנית. קובץ באפס בתים. זו בקשה מפורשת של המשתמש.
8. לשמור את התשובה ב-`תשובות/`.

אם בקשה מסוימת חוסמת או בעייתית - לבצע את כל השאר במלואו, ולומר במפורש
מה הושאר ולמה. לא לצמצם את ההיקף בשקט.

---

## כללי הפרויקט

**מספרי שכר לעולם לא נכתבים ידנית בקוד.** כל ערך נשאב מקבצי המקור שבתיקיית
«נתונים למערכת» דרך `tools/build_dataset.py`. אם ערך צריך להשתנות - מחליפים
את קובץ המקור ומריצים `make`. אין לערוך ידנית את `data/salary_data.json`
או את `web/index.html` - שניהם נוצרים אוטומטית.

**עורכים את `web/template.html`, לא את `index.html`.**
`index.html` בשורש המאגר הוא תוצר בנייה, והוא גם מה ש-GitHub Pages מגיש
בכתובת https://recruitment-demos.github.io/salary-calculator/

**שני מנועי חישוב חייבים להישאר מסונכרנים.** יש לוגיקת חישוב גם ב-
`salary_calc/engine.py` וגם ב-JS שבתוך `web/template.html`. כל שינוי בלוגיקה
חייב להיעשות בשניהם, ו-`tests/test_parity.py` מוודא שהם מחזירים אותו דבר.

**לא לנחש נתונים שאינם במקור.** כשחסר מידע (למשל החזר הוצאות לדרגה שאינה
מתועדת) - להציג אזהרה, לא להשלים בהערכה שנראית כמו עובדה.

**אקסטרפולציה מסומנת תמיד כהערכה** ולא מוצגת כנתון מהסימולציה.

**כשחסר מידע - טווח, לא ניחוש.** `calculate_range` מקבל None לכל פרט שאינו
ידוע ומחזיר מינימום/מקסימום אמיתיים. אין למלא ערך ברירת מחדל בשקט.

**תוספת בל"מ ייעודי תחנות מתועדת בירושלים בלבד** ואין להניח אותה במחוזות
אחרים כשמצב השירות בתחנה אינו ידוע - זה היה מנפח את הגבול העליון.

### פקודות

```bash
make          # בניית הנתונים + עמוד הווב
make test     # 54 בדיקות
make verify   # דוח אי-התאמות בין קבצי המקור
```
